#ifndef _TOWERDEFENSE_IMAGE_UTILS_H_
#define _TOWERDEFENSE_IMAGE_UTILS_H_

#include "cocos2d.h"

namespace towerdefense {

	class ImageUtils
	{
		public:
		//    static Vec2* calculatePosition(Rect s_visibleRect, Sprite *sprite, Vec2 targetPosition) {
		////    	Sprite *background = Sprite::create("towerdefense/art/bg.png");
		////		Rect s_visibleRect = Director::getInstance()->getOpenGLView()->getVisibleRect();
		//		float scaleY = s_visibleRect.size.height / sprite->getContentSize().height;
		//		float scaleX = s_visibleRect.size.width / sprite->getContentSize().width;
		//		if(scaleX>scaleY){
		//			sprite->setScale(scaleX);
		//		}else{
		//			sprite->setScale(scaleY);
		//		}
		//		// background->setPosition( Vec2( winSize.width / 2, winSize.height / 2) );
		//		background->setPosition(Point(s_visibleRect.origin.x+s_visibleRect.size.width/2, s_visibleRect.origin.y+s_visibleRect.size.height/2));
		//    }

		private:

	};

}

#endif // _TOWERDEFENSE_IMAGE_UTILS_H_
